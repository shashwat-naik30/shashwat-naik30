# 🧹 Clean Data

This folder contains the cleaned and transformed datasets used for analysis.

The data was cleaned and prepared using Python and Pandas, including handling missing values, standardizing columns, correcting data types, removing unnecessary fields, and preparing datasets for SQL analysis.
