# 🗄️ SQL Analysis

This folder contains the SQL queries developed to explore, validate, and analyze the election dataset.

A total of 75 SQL queries were created during the project to understand the dataset and derive analytical insights using MySQL.

The queries cover data exploration, filtering, aggregations, joins, subqueries, CTEs, CASE expressions, window functions, and analytical calculations.
